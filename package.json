{
  "name": "microsoft-graphharbor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Endpoint-free agent bridge for Microsoft Teams over Microsoft Graph.",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc",
    "start": "node dist/index.js",
    "prime": "node dist/index.js prime",
    "poll-once": "node dist/index.js poll-once"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}
