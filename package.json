{
  "name": "alice-teams-graph-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Endpoint-free Microsoft Teams chat bridge using Microsoft Graph.",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0"
  }
}

